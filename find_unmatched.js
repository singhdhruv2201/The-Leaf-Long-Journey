const fs=require('fs');const s=fs.readFileSync('main.js','utf8');const stack=[];for(let i=0;i<s.length;i++){const c=s[i];if(c==='{') stack.push(i); if(c==='}') stack.pop();} if(stack.length===0) console.log('No unmatched {'); else { console.log('Unmatched count',stack.length); for(let k=0;k<stack.length;k++){const idx=stack[k]; const before=s.slice(Math.max(0,idx-60),idx); const after=s.slice(idx, idx+60); const linesBefore = s.slice(0, idx).split('\n').length; console.log('Unmatched at index',idx,'approx line',linesBefore); console.log('Context before:\n', before); console.log('Context after:\n', after); }}